import { InspectionRecord } from "../types";

// Serviço desativado para versão sem custos/IA
export const generateInspectionSummary = async (record: InspectionRecord): Promise<string> => {
  return "";
};